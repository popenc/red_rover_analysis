The algorithms folder holds all the external python modules that the red_rover_analysis repository uses.

Note, these are static modules and aren't associated with a repository, so they are as-is and aren't being updated externally, which for our use case is probably for the best.